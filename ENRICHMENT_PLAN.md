# Fake OS — 内容丰富计划 v2.0

> 基于现有功能（启动 → 登录 → 桌面 → 终端/文件管理器/聊天/设置 → 病毒），规划下一阶段丰富方向。
> 每个模块按"优先级 × 趣味值"排序，P0 最重要。

---

## 一、现状评估

### 已实现功能 ✅
- BIOS 启动序列 + 登录界面
- 桌面壁纸 + 图标（可选中/拖拽）
- 窗口系统（拖拽/缩放/最小化/最大化/关闭）
- 任务栏（时钟/信号/电量/开始菜单）
- 终端（20+命令，含 matrix/hack/ping 等）
- 文件管理器（虚拟文件树，隐藏文件夹）
- 聊天软件（5个NPC自动发言，关键词回复）
- 系统设置（外观/隐私/关于/高级含整蛊）
- 病毒.exe（5阶段灾难动画）
- 彩蛋（Konami/F12/凌晨/闲置提醒/桌面点击）

### 待完善项 🔶
- 终端 cd 命令无法真正导航文件系统
- 文件管理器功能单一（无右键菜单/复制/删除）
- 聊天回复深度不足（只匹配少量关键词）
- 全局快捷键缺失
- 桌面小部件缺失
- 应用种类太少（6个）

---

## 二、丰富方向总览

```
第一期（P0）：补全核心体验
├── 终端 cd 导航（联动文件管理器）
├── 快捷键系统（Alt+F4 / Alt+Tab / Win键）
├── 通知系统（右下角弹窗，可堆叠）
├── 桌面时钟小部件
├── 记事本应用（可保存虚拟文件）
└── 计算器应用

第二期（P1）：新增趣味模块
├── 浏览器（假的加载页 + 趣味页面）
├── 画图板（简单 Canvas 绘图）
├── 系统监视器（假的 CPU/RAM 占用率）
├── 屏保程序（2分钟闲置后启动）
├── 右键菜单增强（任务栏/桌面/文件）
├── 壁纸选择器（预设5张风格）
└── Alt+Tab 窗口切换器（带预览）

第三期（P2）：深化叙事与互动
├── 故事模式（线索藏在各个 app 中）
├── 多结局（不止病毒一个结局）
├── NPC 跨应用互动（聊天提到你打开的应用）
├── 隐藏角色"系统维护者"（偶尔出现）
├── 终端更深互动（编辑文件/创建文件）
└── 文件系统彩蛋任务链

第四期（P3）：视觉与体验打磨
├── 窗口模糊/玻璃效果
├── 动态壁纸（粒子系统）
├── 应用图标动画（悬停/打开）
├── 声音反馈（点击/开关/通知）
├── 窗口贴边辅助排列
├── 暗黑模式完整适配
└── 性能优化与代码重构
```

---

## 三、第一期详细计划（P0：补全核心体验）

### P0-1: 终端 cd 导航 + 联动文件管理器 ⭐⭐⭐⭐⭐

**目标**：终端输入 `cd Documents` 能导航到文件夹，`ls` 显示当前目录内容，文件管理器自动同步。

**文件**：`js/terminal.js` 修改

**具体内容**：
```
新增命令：
  cd <路径>     — 导航到文件夹（支持 Documents/Downloads/Pictures/Videos/.hidden）
  mkdir <名>    — 创建目录（假的，说"权限不足"但有彩蛋）
  rm <文件>     — 删除文件（假的，"文件已进入更美好的地方"）
  cp <源> <目标> — 假的，显示"复制成功"但什么都没发生
  mv <源> <目标> — 假的，显示"移动成功"实际上纹丝不动
  open <文件>   — 用对应程序打开（图片→看图、txt→记事本）
  find <关键词> — 搜索文件（彩蛋：搜"宝藏"会找到隐藏文件）
```

**实现细节**：
- 维护一个 `currentPath` 变量（如 `/home/admin/Documents`）
- 修改 `ls` 显示当前路径的内容
- 修改 `pwd` 返回当前路径
- `cd ..` 返回上级目录（`/home/admin` 再上一级会提示"你不能离开这里"）
- 如果文件管理器和终端都打开，切换文件夹时两边同步

**彩蛋**：
- `cd /` → "你不能访问根目录。它太高了。"
- `cd ~` → 回到 `/home/admin`
- `cd` (不加参数) → 显示 "你要去哪儿？"
- `cd ../../..` → "你走得太远了。回来。"

---

### P0-2: 快捷键系统 ⭐⭐⭐⭐⭐

**目标**：常用快捷键提升操作感。

**文件**：`js/easterEggs.js` 或新建 `js/shortcuts.js`

**具体内容**：
| 快捷键 | 功能 |
|--------|------|
| Alt+F4 | 关闭当前聚焦窗口（如果没窗口→弹窗"你想关掉什么？"） |
| Alt+Tab | 简单切换窗口（显示当前窗口列表） |
| Win+R | 打开运行对话框（假的，说"这功能没开通"） |
| Win+D | 显示桌面（最小化所有窗口） |
| Win+E | 打开文件管理器 |
| Ctrl+C / Ctrl+V | 支持文本复制粘贴（系统级） |
| Escape | 关闭弹窗/菜单 |

**实现细节**：
- 维护窗口 z-index 顺序列表
- Alt+Tab 在窗口间循环切换焦点
- Win+D 切换最小化/恢复所有窗口

**彩蛋**：
- Alt+F4 连续按 5 次 → "你是在测试我的耐心吗？"
- Win+R → "运行… 你运行不了我。"

---

### P0-3: 通知系统 ⭐⭐⭐⭐⭐

**目标**：右下角出现通知横幅，可堆叠，可点击关闭。

**文件**：新建 `js/notifications.js`

**具体内容**：
```javascript
function showNotification(title, text, icon) {
  // 在右下角显示通知
  // 3秒后自动消失
  // 可点击关闭
  // 最多同时显示 3 条
}
```

**触发场景**：
- 开机后："欢迎回来。……也许。"
- 聊天有新消息："📩 你有一条新消息"
- 凌晨："🌙 已经凌晨了，该睡了"
- 打开病毒文件后："⚠️ 检测到可疑活动"
- 使用设置里的开关时随机弹出假通知
- 文件管理器删除文件时："🗑️ 文件已删除（并没有）"

**通知队列管理**：
- 最多同时显示 3 条
- 超过 3 条时，旧的通知先消失
- 点击通知执行对应动作（如打开聊天）

**彩蛋**：
- 第 10 条通知 → "你已经收到 10 条通知了。没有一条是重要的。"
- 快速连续弹出时，其中一条是"我能在你背后看到你"

---

### P0-4: 桌面时钟小部件 ⭐⭐⭐⭐

**目标**：桌面上直接显示时间/日期，更显眼。

**文件**：`js/desktop.js`（添加 widget 渲染）

**具体内容**：
- 桌面右上角显示：
  ```
  14:23
  星期五 2026年6月12日
  ```
- 字体半透明，鼠标悬停稍微变亮
- 点击打开日历（假的，显示当月但月份会随机跳跃）

**样式**：字体优雅、带微弱的发光阴影

**彩蛋**：
- 日期每 10 秒快进一天
- 点击 5 次 → "你赶时间吗？"
- 午夜 0:00 → 额外显示 "🎉 新的一天……还是一样"

---

### P0-5: 记事本应用 ⭐⭐⭐⭐

**目标**：可以打开/编辑/保存虚拟文件。

**文件**：新建 `js/notepad.js`

**具体内容**：
- 应用图标：📝
- 窗口包含文本编辑区 + 工具栏（保存/另存为/字体大小）
- 从终端 `open 老板的黑历史.txt` → 用记事本打开
- 从文件管理器双击 .txt 文件 → 用记事本打开
- 保存时显示"文件已保存（但这是假的，什么都没存）"
- 可以新建空白文件

**实现细节**：
```javascript
createWindow("notepad", "📝 记事本", 500, 350, `
  <div style="display:flex;flex-direction:column;height:100%;">
    <div style="padding:8px;border-bottom:1px solid rgba(255,255,255,0.06);">
      <button onclick="notepadSave()">💾 保存</button>
      <button onclick="notepadNew()">📄 新建</button>
    </div>
    <textarea id="notepad-text" style="flex:1;background:transparent;border:none;color:#fff;padding:12px;font-family:monospace;font-size:14px;resize:none;outline:none;"></textarea>
  </div>
`);
```

**彩蛋**：
- 输入 "FakeOS" → 自动补全 "是最好的操作系统（才怪）"
- 输入 "Hello" → "World" 自动补全
- 保存时文件名如果是 "日记" → "你写日记吗？小心被人看到"
- 第 5 次保存 → 弹窗 "你已经保存 5 次了。但什么都没有保存。"

---

### P0-6: 计算器应用 ⭐⭐⭐

**目标**：一个能用的计算器，偶尔出错。

**文件**：新建 `js/calculator.js`

**具体内容**：
- 标准计算器布局（0-9、+、-、×、÷、=、C）
- 按键有点击动画
- 计算逻辑：前 10 次计算正确，之后偶尔算错（2+2=5）

**彩蛋**：
- 按 0 五次 → 屏幕显示 "∞"
- 按 1+1 三次 → 第三次结果 = "3"
- 输入 6×9 → "42"（致敬银河系漫游指南）
- 按 C 十次 → 显示 "你清不掉我的。我是你的一部分。"

---

## 四、第二期简要计划（P1：新增趣味模块）

### P1-1: 浏览器（假的）
- 地址栏输入 URL → 加载动画 → 显示预设页面
- 内置 "页面"：google.com（FakeOS 版）、youtube.com（全是猫视频）、github.com（FakeOS 仓库，404）
- 彩蛋：输入 about:blank → "空白页。就像你的浏览器历史。"
- 彩蛋：输入 127.0.0.1 → "那就是我。"

### P1-2: 画图板
- Canvas 绘图，简单画笔工具
- 6 种颜色 + 橡皮擦
- 保存功能（假的，保存时显示"杰作已保存"但实际没了）
- 彩蛋：用红色画满屏幕 → "你创作了一件现代艺术"

### P1-3: 系统监视器
- 假的 CPU 占用率（波浪动画，随着打开应用增多而上升）
- 假的 RAM 使用量（永远显示 "87%"）
- 假的进程列表（包含 "system.exe"、"fakeos.exe"、"you.exe"）
- 彩蛋：找到一个叫 "secret.exe" 的进程 → 点它 → 打开病毒

### P1-4: 屏保程序
- 2 分钟无操作触发
- 三种随机屏保：星空、弹跳球、滚动代码
- 移动鼠标或按任意键退出
- 彩蛋：屏保中出现隐晦的文字

### P1-5: 桌面右键菜单增强
- 新建文件夹（假的，显示"创建成功"但实际没有）
- 刷新（桌面闪一下）
- 显示设置（弹窗）
- 个性化（弹窗）

### P1-6: 壁纸选择器
- 预设 5 张壁纸（纯 CSS 渐变/图案）
- 缩略图预览
- 深色/浅色切换联动
- 彩蛋：第 5 张壁纸选中后偶尔闪烁

### P1-7: Alt+Tab 切换器
- 按 Alt+Tab 时显示窗口缩略列表
- 可循环切换
- 带窗口标题和图标

---

## 五、第三期简要计划（P2：深化叙事与互动）

### P2-1: 故事模式
- 背景设定：你是一家中等公司 IT 部门的实习生
- 打开电脑后发现前员工留下的奇怪系统
- 每个应用里藏着故事碎片
- 文件管理器里有"离职同事的日记.txt"
- 聊天记录里有前员工和神秘人的对话

### P2-2: 多结局系统
- 结局 1（当前）：病毒.exe → 格式化
- 结局 2：收集所有线索 → 解锁"真相"结局
- 结局 3：某些特殊操作 → "逃离"结局
- 结局 4：什么都不做 5 分钟 → "你还在等什么？"结局

### P2-3: NPC 跨应用互动
- 聊天 NPC 会说："我看到你在用计算器。连 2+2 都能算错？"
- 终端命令会影响聊天内容
- 设置里的开关会触发 NPC 的反应

### P2-4: "系统维护者"隐藏角色
- 偶尔在终端出现的神秘用户
- 会发消息说"要帮忙吗？"
- 如果你说"好" → 它会修复一些"问题"（窗口排列变整齐、壁纸变正常）
- 如果你说"不" → "好吧。你一个人也能搞定。"

### P2-5: 终端创建/编辑文件
- `edit 文件名.txt` 打开记事本编辑
- `touch 文件.txt` 创建空文件
- `rm -rf` 终极彩蛋（假装删除所有内容，实际只是清屏）

---

## 六、第四期简要计划（P3：视觉与体验打磨）

- 窗口背景半透明磨砂效果（backdrop-filter: blur）
- 粒子动态壁纸（canvas 实现的星云/飘落）
- 图标动画（鼠标悬停时轻微浮动）
- 全局过渡动画统一
- 声音反馈系统（Web Audio API 生成简单点击音）
- 窗口贴边半屏排列
- 完整暗黑模式（目前只有红色夜间模式）
- 键盘导航（Tab 切换焦点）
- 代码重构与模块化

---

## 七、优先级排序与工作量估算

| 编号 | 内容 | 优先级 | 估算文件数 | 预计代码行 | 趣味值 |
|------|------|--------|-----------|-----------|-------|
| P0-1 | 终端 cd + 联动 | P0 | 2 | ~150 | ⭐⭐⭐⭐⭐ |
| P0-2 | 快捷键系统 | P0 | 1 | ~100 | ⭐⭐⭐⭐⭐ |
| P0-3 | 通知系统 | P0 | 1 | ~120 | ⭐⭐⭐⭐⭐ |
| P0-4 | 桌面时钟 | P0 | 1 | ~50 | ⭐⭐⭐⭐ |
| P0-5 | 记事本 | P0 | 1 | ~100 | ⭐⭐⭐⭐ |
| P0-6 | 计算器 | P0 | 1 | ~120 | ⭐⭐⭐ |
| P1-1 | 浏览器 | P1 | 1 | ~150 | ⭐⭐⭐⭐⭐ |
| P1-2 | 画图板 | P1 | 1 | ~200 | ⭐⭐⭐⭐ |
| P1-3 | 系统监视器 | P1 | 1 | ~120 | ⭐⭐⭐⭐ |
| P1-4 | 屏保 | P1 | 1 | ~80 | ⭐⭐⭐ |
| P1-5 | 右键增强 | P1 | 1 | ~60 | ⭐⭐⭐ |
| P1-6 | 壁纸选择器 | P1 | 1 | ~100 | ⭐⭐⭐ |
| P1-7 | Alt+Tab | P1 | 1 | ~80 | ⭐⭐⭐⭐ |
| P2-1 | 故事模式 | P2 | 3 | ~300 | ⭐⭐⭐⭐⭐ |
| P2-2 | 多结局 | P2 | 2 | ~200 | ⭐⭐⭐⭐⭐ |
| P2-3 | 跨应用互动 | P2 | 3 | ~150 | ⭐⭐⭐⭐ |
| P2-4 | 隐藏角色 | P2 | 2 | ~100 | ⭐⭐⭐⭐ |
| P2-5 | 终端文件编辑 | P2 | 2 | ~100 | ⭐⭐⭐ |
| P3-1 | 视觉打磨 | P3 | 3 | ~300 | ⭐⭐⭐⭐ |
| P3-2 | 声音系统 | P3 | 1 | ~100 | ⭐⭐⭐ |

---

## 八、推荐执行顺序

```
Week 1: P0-1 (终端cd) → P0-2 (快捷键) → P0-3 (通知)
Week 2: P0-4 (时钟) → P0-5 (记事本) → P0-6 (计算器)
Week 3: P1-1 (浏览器) → P1-2 (画图板) → P1-3 (监视器)
Week 4: P1-4 (屏保) → P1-5 (右键) → P1-6 (壁纸) → P1-7 (Alt+Tab)
Week 5+: P2 叙事深化 + P3 视觉打磨（按兴趣选做）
```

目前先推荐从 **第一期（P0）** 开始，搞定核心体验的短板。

---

*文档版本: v2.0 | 创建时间: 2026-06-12*
